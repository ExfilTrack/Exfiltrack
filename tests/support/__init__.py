"""Shared test-support helpers (fixture builders), not tests themselves.

Nothing under here is collected by pytest; it exists so both
``tests/unit`` and ``tests/integration`` can build synthetic evidence the
same way instead of duplicating it.
"""
