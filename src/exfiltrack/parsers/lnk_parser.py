"""Windows shortcut (.lnk) file parser.

Owner: Thabrew
Related issue: #5 - LNK Parser

Planned scope:
    - Parse .lnk shortcut files read-only
    - Extract target file paths, sizes, and volume identifiers
    - Extract creation, access, and modification timestamps
    - Emit normalized events with parser name and version

Not implemented yet.
"""

PARSER_NAME = "lnk_parser"
PARSER_VERSION = "0.0.0"
